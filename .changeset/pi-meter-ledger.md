---
"@zhcsyncer/pi-extensions": minor
"@zhcsyncer/pi-meter": minor
---

Add `@zhcsyncer/pi-meter`: a local usage ledger plus subscription remaining. The below-editor chrome stays independent of Glance, SuperGrok uses the verified Grok CLI billing JSON, and `@pi-plugins/usage` must be disabled because both register `/usage`.
