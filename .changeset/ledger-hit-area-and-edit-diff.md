---
"@zhcsyncer/pi-tool-display-intent": minor
---

Expand the clickable area to the whole collapsed Tools block and the expanded title/summary area, while preserving per-call detail inspection. Successful Edit calls with returned diff data show an automatically wrapped, single-column diff directly in Result using the existing line-numbered diff renderer. Raw return text and arguments remain accessible; failed edits and missing diff data show the ordinary result.
