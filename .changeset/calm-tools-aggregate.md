---
"@zhcsyncer/pi-tool-display-intent": minor
"@zhcsyncer/pi-extensions": minor
---

Add the optional `toolCalls.layout: "aggregate"` Activity view. Owned safe built-in tools can now collapse into one non-expandable, branch-aware status row per user request while preserving raw session calls/results and the complete existing `individual` layout.
