---
"@zhcsyncer/pi-tool-display-intent": minor
"@zhcsyncer/pi-extensions": minor
---

Add the optional `toolCalls.layout: "aggregate"` Activity view. Owned safe built-in tools can now collapse into one branch-aware status row per user request, use distinct theme-aware tool and diff colors, retain successful calls as replaceable `done` rows before a settled grace-period fold, suppress pure collapsed-thinking placeholders, and expose only bounded per-file change summaries through `Ctrl+O`, while preserving raw session calls/results and the complete existing `individual` layout.
