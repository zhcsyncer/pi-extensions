---
"@zhcsyncer/pi-extensions": patch
"@zhcsyncer/pi-tool-display-intent": patch
---

Keep the aggregate Tools ledger a bounded audit: custom tools show a deterministic target, image reads stay in the ledger, and long bash/tool-call rows wrap with a preview instead of dumping scripts or failing open.
