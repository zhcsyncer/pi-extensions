---
"@zhcsyncer/pi-tool-display-intent": patch
---

Aggregate keeps a blank line after the user prompt and passthrough tools; only the final reply under Tools omits it. Ctrl+O also leaves a framed gap before mid-turn narration. `/tools` now Tab-completes `aggregate` and `individual`.
