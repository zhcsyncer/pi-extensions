---
"@zhcsyncer/pi-extensions": minor
"@zhcsyncer/pi-subagents": minor
---

Render Agent / get_subagent_result / steer_subagent transcript rows as unboxed Claude Code Task chrome (`● Type(description)` + a single `⎿` outcome clerk). Collapsed rows keep outcome stats and the resolved model; spawn config, cost, transcript path, and worktree details move to the expanded footer.
