---
"@zhcsyncer/pi-extensions": minor
---

Bundle an internal fork of `@juicesharp/rpiv-todo` 1.20.0. Todo keeps branch-aware tool-result snapshots but hides successful transcript nodes in favor of its persistent widget, while preserving visible errors and intentionally avoiding display-intent metadata.
