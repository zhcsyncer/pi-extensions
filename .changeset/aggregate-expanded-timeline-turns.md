---
"@zhcsyncer/pi-tool-display-intent": patch
---

Aggregate Ctrl+O can group the timeline by agent turn. The default stays the flat per-call view; turn `/tools` Expanded timeline to `turns` without reloading.
