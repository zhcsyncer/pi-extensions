---
"@zhcsyncer/pi-glance": minor
"@zhcsyncer/pi-extensions": minor
---

Add a theme-aware Git Working Tree summary that defaults into the Git status line, with an optional bottom-right border placement, tracked diff statistics, event-driven refresh with polling fallback, and a `/diff` revdiff review handoff that returns annotations to the editor for confirmation.
