---
"@zhcsyncer/pi-herdr-companion": minor
"@zhcsyncer/pi-extensions": minor
---

Add the standalone `@zhcsyncer/pi-herdr-companion` package with immutable/degraded-aware Herdr runtime context, branch-safe owned `herdr_process` panes with foreground-process reconciliation, durable `/btw` side threads with best-effort cache-aware replay and request-deduplicated single-owner recovery (including the documented fire-and-forget crash window), and balanced Ask User Question blocked-state adaptation. The root tarball embeds the package sources for release consistency but does not auto-enable the extension.
