---
"@zhcsyncer/pi-herdr-companion": minor
"@zhcsyncer/pi-extensions": minor
---

Add the standalone `@zhcsyncer/pi-herdr-companion` package with immutable/degraded-aware Herdr runtime context, branch-safe owned `herdr_process` panes with foreground-process reconciliation, durable `/btw` side threads with bounded Herdr 0.8 fresh-shell launch retries, best-effort cache-aware replay, request-deduplicated single-owner recovery (including the documented fire-and-forget crash window), and accepted-mailbox cleanup before exact child pane closure, plus balanced Ask User Question blocked-state adaptation. The root tarball embeds the package sources for release consistency but does not auto-enable the extension.
