---
"@zhcsyncer/pi-herdr-companion": minor
"@zhcsyncer/pi-extensions": minor
---

Add the standalone `@zhcsyncer/pi-herdr-companion` package with immutable Herdr runtime context, ownership-safe `herdr_process` panes, durable `/btw` side threads with cache-aware context replay and exactly-once merge recovery, and balanced Ask User Question blocked-state adaptation. The root tarball embeds the package sources for release consistency but does not auto-enable the extension.
