---
"pi-provider-volcengine-agent-plan": patch
---

Stop crashing on Pi 0.86+ when a built-in catalog id is missing or renamed (DeepSeek Flash is now `deepseek-flash`). Agent Plan still keeps its own route ids.
