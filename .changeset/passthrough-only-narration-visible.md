---
"@zhcsyncer/pi-extensions": patch
"@zhcsyncer/pi-tool-display-intent": patch
---

In aggregate layout, keep pre-tool narration visible when that assistant message only has passthrough tools such as Consult, instead of hiding it until expand.
