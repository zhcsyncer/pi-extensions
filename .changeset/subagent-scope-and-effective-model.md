---
"@zhcsyncer/pi-subagents": patch
"@zhcsyncer/pi-extensions": patch
---

Apply scopeModels to explicit cross-extension RPC model selections, close the conversation viewer with Ctrl+C, and display the child session's actual model and thinking level rather than requested values. Keep foreground as the default; no workflow or handle-based delegation features are added.
