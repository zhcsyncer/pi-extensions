---
"@zhcsyncer/pi-tool-display-intent": patch
---

Add Bash intent language to the `/tools` settings panel in both layouts, with explicit guidance that auto is best-effort and changes apply after `/reload`.
