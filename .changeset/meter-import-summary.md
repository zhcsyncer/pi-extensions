---
"@zhcsyncer/pi-meter": patch
"@zhcsyncer/pi-extensions": patch
---

Make `/usage import` report scanned files, parsed records, additions, known duplicates, skipped messages, and whether a repeated import changed the ledger.
