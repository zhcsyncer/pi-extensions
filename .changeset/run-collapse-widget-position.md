---
"@zhcsyncer/pi-tool-display-intent": patch
---

Right-align the fixed Run collapse control and keep it first among above-editor widgets, without changing other widgets' relative order. Match the mouse hit area to the visible control so the empty left side remains non-interactive.
