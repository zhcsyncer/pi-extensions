---
"pi-provider-cursor-ask": patch
---

Successful Cursor recoveries and protocol noise no longer print a Warning in the chat transcript, and incomplete billing no longer occupies the footer. Those events still go to the lifecycle log; `/cursor usage` and `/cursor doctor` are unchanged.
