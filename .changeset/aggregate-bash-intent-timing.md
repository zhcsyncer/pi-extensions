---
"@zhcsyncer/pi-extensions": patch
"@zhcsyncer/pi-tool-display-intent": patch
---

In aggregate layout, long bash rows show intent and size instead of the script body, and each call row right-aligns its duration (plus end time after it finishes).
