{
  "routing": "priority",
  "priority": ["exa", "tavily", "firecrawl", "parallel"],
  "compact": false,
  "backends": {
    "exa": { "enabled": true, "apiKeys": ["EXA_API_KEY"] },
    "tavily": { "enabled": true, "apiKeys": ["TAVILY_API_KEY"] },
    "firecrawl": { "enabled": true, "apiKeys": ["FIRECRAWL_API_KEY"] },
    "parallel": { "enabled": true, "apiKeys": ["PARALLEL_API_KEY"] }
  }
}
